# Architecture Decision Records (ADRs)

Fixture: not a decision. `parse` reads ADRs by their `NNNN-*.md` name, so this file yields nothing.

- **Builds on:** this bullet is not in an ADR, so it is never read
