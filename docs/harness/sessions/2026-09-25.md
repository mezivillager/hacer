# Session record — 2026-09-25: the weekly process sweep, and two projects kicked off from idea to live in one run

This is the committed memory of the 2026-09-25 session. It lets the work resume from the repo alone.
What to do next lives in `docs/portfolio.md` and GitHub Issues, never here.

## 1. The goal

The standing goal is unchanged (`sessions/2026-09-18.md` §1). The owner's instruction for this run:
*"get the platform in a better shape, kicking off those priority projects, identifying and fixing
overall process defects … more focusing on healing and streamlining the process, than in
coordinating queued up code change related items … consider this as a once a week process sweep
(with the exception being you're also setting up and kicking off those 2 new important projects)."*
He steered twice mid-run — "keep going" when the coordinator paused for the weekly meter, then
"keep going until weekly usage is 99%" — and was away otherwise. Publishing stayed `on-green`.

## 2. The two projects, from parked idea to live

The day before, the owner had parked two ideas (#457, #458) with the instruction that they move only
when he asks. He asked, and asked for better names. Two research reports
(`docs/research/2026-09-24-decision-lineage/`, `docs/research/2026-09-24-mission-control/`) proposed
the names, the shape, the tasks and the pick-rule slot; he answered "yes to all"; PR #464 landed the
research and PR #486 the portfolio rows 13/14 and the eight-slot cycle
`foundation → lineage → harness → foundation → mission-control → spine → foundation → aux`.

**Decision Lineage** (epic #457, `project:lineage`) — decisions as a graph you can walk.

| Landed | What it gives |
|---|---|
| #487 DL-1 | the parser: `ADR-NNNN` / `R<n>` / `P-<n>` nodes, five edge kinds, the field block, `none` vs `unknown` |
| #490 DL-1b | **all 342 rulings since 2026-09-18 imported** into `docs/decisions/rulings/`, one file per run; 40 with real lineage, 299 honestly `unknown` — the lineage ratchet's starting number |
| #503 DL-2 | `lineage trace` / `radius` / `check --fix` / `graph`; `lint:lineage` in warn mode prints `LINEAGE: 390 decisions · 299 unlinked · 0 unresolved · 9 superseded-cited`; the four missing reverse links written into the ADRs |
| #510 DL-4 | the briefs ask for `Decisions:` and `Invalidates:`, the templates for `Introduced by:`, the ledger has `Id` (L001–L055) and `Decision` columns |
| #514 DL-5 | `lineage correct <id>` — one draft issue per downstream decision, `--file` stubbed-only in tests; round 2 in flight at close (a retry after a partial failure must not duplicate) |

Queued for Grok Bot's cloud lane: **#468 DL-3** (executable premises; its day-one test is that
P-001 — the R3F peer range — reports *expired*). Later: DL-6 (fail mode, two weeks after DL-4's
cut-over), DL-7 (the graph in Mission Control).

**Mission Control** (epic #458, `project:mission-control`) — **live at
`https://mezivillager.github.io/hacer/control/`**, a read-only projection of the platform's own state.

| Landed | What it gives |
|---|---|
| #488 MC-1 | the collector: one versioned `snapshot.json` from GitHub + the repo's files, reusing `backlog.logic.mjs` so it can never disagree with `ready`; per-section freshness; ~6 GraphQL requests a run |
| #501 MC-2 | the site: Overview and Projects, hash routes, a stale banner, deployed by the existing Pages pipeline with PR previews; the app bundle byte-identical |
| #507 MC-6 | the three required checks publish their summary lines as public annotations; the collector reads them **with the run's conclusion as the verdict** |
| #511 MC-3 | the Process view — the loop with live stage counts, claims joined to issue state, the cloud lane |
| #512 MC-5 | the hourly refresh workflow, a history archive on `gh-pages` (first file `control/history/2026-09-25T07:07:45.960Z.json`), the freshness banner |
| #515 MC-4 | the Timeline view — sessions, merges and releases on one axis over a 14-day window |

Later: MC-7 charts, MC-8 roadmap/ledger/ADR views, MC-9 the first editable step (prefilled issue
forms), DL-7 the lineage graph.

## 3. The sweep — process defects found and fixed

| Defect | Fixed by |
|---|---|
| Five `claim/*` refs pointed at closed issues; #193 was closed while Projects 2–5 remained (its PR said `Part of`) | refs released, #193 reopened; **#499** makes `Fixes #n` + "in part" a `pr-hygiene` FAIL, with a Markdown reader that matches GitHub's renderer on 244/244 of our bodies and 3,443 of 3,500 public ones |
| 27 of the last 60 merges had no `## Verifier verdict:` comment — mostly docs and Dependabot, but five cloud-lane merges were verified in a format the regex could not see | **#492**: the heading is the cross-lane contract; ledger rows corrected against the artefacts; the brief gains the test-only path, the claim comment, two guidance lines |
| 29 preview folders on `gh-pages` for 3 open PRs | **#491**: a daily sweep, removing only on positive evidence that a PR is closed (round 1 would have wiped everything on an empty `gh` list); first run 33 → 4 |
| Two tests read the live ratchet baseline and decayed as it shrank — one went vacuous | **#498** |
| The ratchet guard's config scan had no self-check | **#461** fails closed (`ratchet=unreadable`) |
| An emptied `.dependency-cruiser.cjs` passed `lint:layers`; a config-only PR was never read | **#508**: `UNDECLARED` fails `lint:layers`; `ratchet=disarmed` for a config-only deletion; #456 closed; narrowing and `options.exclude` filed as #505 |
| The `lint-staged` major (#496) would have merged on green though CI never runs the hooks | held, with the hook check written on the PR |

Filed, not fixed: #489→done, #500, #502 (cosmetic), #504, #505, #506 (a gate drag whose re-route
fails deletes the wire — pre-existing, found probing #459), #509, #513, **#516** (a lint for case-only
filename collisions — two Mission Control PRs hit it in one day: `process.ts`/`Process.tsx`,
`timeline.ts`/`Timeline.tsx`; macOS resolves the wrong module while Linux CI passes).

Also merged: **#459** (removeJunction keeps every wire with two real endpoints — B-009 resolved on
its third verification attempt), Dependabot #493/#494/#495, and the cloud rows PR #497.

## 4. What recurred, and what caught it

- **The #394 class again — in the run's own first ruling.** R700 assumed the weekly meter had reset
  because the local date had rolled over; the browser was unresponsive, and the ruling was written on
  the assumption. The meter read 93%. R711 amended it; under DL-3 that premise (P-006) would have
  carried a verify command and the run's first action would have run it. Recorded as a lineage chain
  R700 → R711 → R712 → R720 → R726, which is now itself in the graph.
- **A required check that read green for a failed run.** MC-6's first round parsed the published
  line and ignored the run's conclusion; the #508 verifier noticed as a nit, the #507 verifier
  confirmed it as a BLOCK. The conclusion now wins — the tool reads the verdict, not the sentence.
- **Tests no project collected**, twice: #436 (yesterday) and #512's six threshold tests (`.ts` under
  `mission-control/`). Round 2 added a test that lists what vitest collects.
- **The stuck merge box after a no-op `update-branch`** (#486, #511) — second occurrence; the manual
  fix is a fresh-SHA re-push; `gh-merge-on-green` needs to do it itself.
- **A builder's `git fetch --depth=1` made the shared `.git` shallow** for every worktree until
  repaired; every brief now says never to.
- **Lane drift in verdict format**; **`Fixes` closing issues in part** — both mechanised this run.

## 5. Rulings

R700–R740, in `docs/decisions/rulings/2026-09-25-process-sweep.md`, every one with `Builds on:` and a
cost-if-wrong — the first run recorded in the format it built. The ones that changed outcomes: R600's
successor R700/R711/R712/R720/R726 (the usage ceiling and its false premise), R706 (sweep items go
through builders, not coordinator edits), R715/R723 (positive closure for the preview sweep),
R727/R728 (`withoutCode` must equal GitHub's reader), R732 (#456 closes; narrowing → #505),
R734 (conclusion wins), R737 (stuck merge box, second occurrence), R740 (`--file` resumable).

## 6. State at close

Merged this run: **24 PRs** (#464, #486, #461, #487, #488, #492, #497, #493, #494, #495, #490, #491,
#498, #499, #503, #501, #459, #508, #510, #507, #511, #512, #515, and the session record). Open:
#514 (DL-5 round 2), #496 (held), #410 (awaiting Dependabot's recreate). Claims: only #193 (Grok
Bot's), #182 (paused, red commit only), and #470 while round 2 runs.

**Owner's questions, with the defaults taken:** the "99%" instruction was read as last week's meter
(it ended at ≤99% with no cap hit); this week ran under 70% and finished near 12% — say if 99% was
meant for every week. `OWNER-SKILL-EDIT.md` in the run directory carries the one-path edit to the
global `autonomous` skill (rulings written into `docs/decisions/rulings/` from the start of a run).

**How to resume:** `node scripts/backlog.mjs ready`, then `docs/portfolio.md`; Mission Control's
Overview at `/control/` shows the same numbers. The next process sweep should start from
`lint:lineage`'s line and the Mission Control Process view rather than from a survey agent.
