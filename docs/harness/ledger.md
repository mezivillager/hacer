# Failure ledger

One line per thing that went wrong in the loop. The **second** time the same thing appears, it
becomes a mechanism — a lint, a test, a hook, a check — and the row links to it. Only what cannot be
mechanised goes into `AGENTS.md`, under its line budget.

Format: `date · what went wrong · which layer should have caught it · mechanised? (link or "no")`

| Date | What went wrong | Should have been caught by | Mechanised? |
|---|---|---|---|
| 2026-09-18 | Four statements in the workspace `CLAUDE.md` were stale for months (HDL pipeline "not wired", `gateLogic.ts`, Stryker, E2E in the DoD); every session started from a wrong picture | a does-this-path-exist check on cited files (#153) | no — #153 |
| 2026-09-18 | The `main-rules` ruleset could only be merged through by admin bypass (1 approval a solo owner cannot give; `required_deployments` and `code_scanning` rules no PR could satisfy); AGENTS.md claimed CI was required — it was not | nothing; a settings audit | yes — ADR-0013; ruleset rewritten, bypass removed after #234 |
| 2026-09-18 | First research draft kept the owner as the merge bottleneck and proposed label gates the owner's own identity defeats | an adversarial review before acting | yes — every research deliverable gets a fresh-context adversarial review + fact-check |
| 2026-09-18 | Research draft rested a goal-post exit test on a `lint:docs` capability that does not exist (it only rejects absolute paths) | fact-check | yes — reviews; the capability itself is #153 |
| 2026-09-18 | Tracer PR #238: the pre-commit `tsc -b` makes a compile-broken "red" commit impossible; the builder committed the test file from a green tree instead | `ha-prompt-it` should say how to write a red that compiles | no — fix in v2 (#154): red = test + a compiling stub that fails, never `--no-verify` |
| 2026-09-18 | `pnpm install` in every worktree prints `husky: command not found` (husky is not a devDependency; hooks still run via `core.hooksPath`) | the worktree script (#157) | yes — #157: `scripts/wt-new` prints a one-line note that the message is expected |
| 2026-09-18 | Tracer PR #238: 11 builder tests + a green DoD missed that the engine read leftover pin state (a table could differ before and after save/reload); the fresh-context verifier found it with a repro and a 2-line fix | the verifier — and it did | yes — `verifier-brief.md` step 6 ("try to break it") is why it was found; keep it |
| 2026-09-18 | Issue #149's verification command named an exact new test file (`scripts/backlog.test.mjs`); the builder followed the repo's `x.logic.test.mjs` convention, so the command as written exited 1 | the issue form: verification commands should target a directory or pattern, not a file that does not exist yet | no — add to the issue-form guidance (#154) |
| 2026-09-18 | Ticket P05-31 (6 h, engine + store + UI) would have been one ~600-line PR under the old rule; split into #164/#235/#236 it shipped as a 163-line PR in 34 minutes | ADR-0013 one-sub-issue-per-PR | yes — ADR-0013; `pr-hygiene` (#150) will enforce the budget |
| 2026-09-18 | `strict` up-to-date policy: every PR needs a rebase + fresh CI (~2 min) once `main` moves; with three PRs landing in an hour that was three extra CI runs | expected; a cost, not a defect | n/a — revisit if it becomes the bottleneck |
| 2026-09-18 | `pr-hygiene` was made a required check after being tried only on human PRs; every dependabot PR then failed its linked-issue rule | trying a new required check on every *kind* of PR that exists (human, agent, bot) before requiring it | yes — bot/`dependencies` exemption (#250); the rule itself: ADR-0013 amendment candidate |
| 2026-09-18 | Removing the admin bypass broke every release: `@semantic-release/git` pushes a commit to `main`, now rejected (`GH013`); five release runs failed before anyone looked | an audit of every workflow that pushes to `main` before changing the ruleset; `agent-orient` showing the release workflow's status | partly — ADR-0015 (releases no longer commit to `main`); release status in `agent-orient` is #156 |
| 2026-09-18 | The agents' `gh` token lacked the `workflow` scope, so they could not update or auto-merge any PR touching `.github/workflows/**` (dependabot #118/#131, and a cosmetic `ci.yml` hunk in #245) | a token-scope check at session start | yes — owner ran `gh auth refresh -s workflow`; agent PRs still avoid incidental workflow edits |
| 2026-09-18 | `strict` required-status-checks made every merge put every other green PR `BEHIND`; ten PRs in two hours meant a rebase + CI cycle each, and dependabot PRs re-triggered each other | measuring merge throughput after the ruleset change | yes — `strict` off; `ci.yml` still runs on every push to `main` |
| 2026-09-18 | PR #244 added an AGENTS.md line citing `pr-hygiene.logic.mjs` without its `scripts/` prefix; the path-exists check (#245) caught it as soon as its branch was rebased onto that `main` | the path-exists check — before it had even merged | yes — ADR-0014 |
| 2026-09-18 | The eslint-plugin-react-hooks 7.1.1 bump (dependabot #114) failed lint with four real "Cannot access refs during render" findings | the React Compiler lint rules, on the bump itself | no — #256 fixes the code; the bump waits on it |
| 2026-09-18 | The browser-qa builder kept polling its own CI after its PR merged (~300k tokens) | the implementer brief | yes — "agents stop after opening their PR" (Budgets, #292) |
| 2026-09-19 | A builder's `git fetch` hung (a ref-lock race with another worktree fetching at the same moment); the agent sat idle until it was stopped and resumed | a fetch timeout in the worktree script | yes — #157: `scripts/wt-new` bounds `git fetch` to 60s (`timeout`/`gtimeout`) and names the likely cause instead of hanging |
| 2026-09-19 | The default subagent model was switched to Sonnet to cut cost without asking; the owner corrected it (Opus for significant work, Sonnet only for routine work) | asking before changing a cost-versus-quality knob | yes — Budgets table + `CLAUDE_CODE_SUBAGENT_MODEL=opus` (#292) |
| 2026-09-19 | #287 stayed `BLOCKED` with every required check green: a PR created with labels fires `opened` and `labeled` together, `cancel-in-progress` cancels one run per workflow, and the cancelled `pr-hygiene`/`browser-qa` contexts counted as failures | trying a new required check on a PR created with labels | yes, four days and five recurrences later — #295; see the 2026-09-23 row |
| 2026-09-19 | The first cloud-spike routine did all its work, then could not push (403: the Claude GitHub App had no write access), so its commit died with the container | a write-access check before a long cloud run | yes — a one-minute write-access routine now comes first; `docs/harness/cloud-session-spike.md` |
| 2026-09-19 | Connecting Cursor to GitHub switched on Bugbot (billed per use) for every PR without the owner finding the setting; it reviewed #294 | checking what an integration enables right after connecting it | no — the owner turns it off; the repo side goes with #258 |
| 2026-09-21 | The definition of done had drifted past the three versions on record: a grep found ten live restatements (`CLAUDE.md` ×2, `AGENTS.md` ×2, `.cursorrules`, `.github/copilot-instructions.md`, `REPO_MAP.md`, `docs/llm-docs-sync.md`, `docs/roadmap/README.md`, `verifier-brief.md`), one down to two commands with `build` missing entirely | no map from rule to owning file existed to catch new restatements as they were added | yes — `docs/harness/COVERAGE.md` (#300); every site above now links to `implementer-brief.md` instead of restating |
| 2026-09-21 | Nothing held the briefs to their own rules: a rewrite could drop "no AI attribution trailers", one of the five definition-of-done commands or "BLOCK only with `file:line`" and every later agent would read the weakened brief as the contract — the way the workspace `CLAUDE.md` carried Stryker and E2E-in-the-DoD for months (row 1) | a test that pins the live briefs, the way `scripts/backlog.logic.test.mjs` pins `docs/portfolio.md` | yes — #299: `scripts/skills.logic.mjs` + `scripts/skills.logic.test.mjs`; `lint:docs` now also checks citations in the briefs and in the skills we own |
| 2026-09-21 | A Haiku 4.5 verifier replay of #238 returned the right verdict *letter* while missing the defect entirely — it marked the broken criterion satisfied, citing a test whose name matched it — and blocked instead on a timeout in a UI test the PR never touches | nothing: verdict letters were all that was being compared, and the miss showed only because the replay was scored on the defect | yes — `model-tiering.md` (#255), plus two rules of evidence in `verifier-brief.md`: a criterion needs a test you ran, and a failure you cannot tie to the diff is a machine problem until re-run at `origin/main` |
| 2026-09-21 | An unrationed research burst spent ~17M Cursor tokens in 40 minutes — about 49 reviews, or three days of even use at the measured 348K per review — against a pool whose size Cursor does not publish; nothing would have shown it until the lane simply stopped working | nothing: the per-run audit row existed (#296) but no one summed it, and there was no budget to sum it against | yes — #302: `docs/harness/usage-rationing.md` + `scripts/lane-budget.logic.mjs`, checked by `second-opinion.mjs` before every run (exit 7 = skip the lane) |
| 2026-09-21 | Six rules the harness already relied on informally were never written down — measurement discipline, the worker-role framing, reuse-first, a Not-covered list and stopping condition in the review briefs, and an ADR reuse-considered section — so a rewrite or a fresh-context agent had no fixed text to be held to | nothing: each was a habit, not text an agent could be held to | yes — #301 |
| 2026-09-21 | A plan draft made a reviewer's suggestion — freeze the legacy canvas, rank 2D ahead of 3D — the default, although the owner had said remove and had not ranked the surfaces; he overruled it the same day | the coordinator: a review can correct facts, it cannot change the owner's direction | no — a rule of judgment: `docs/harness/sessions/2026-09-21.md` §7 |
| 2026-09-21 | With the machine capped at 3 agents, one research agent forked helpers and held every slot for half an hour while a builder waited; a message asking it to stop arrived after it had forked again | the dispatch brief: say "no helpers" when the cap matters | no — first occurrence; briefs for research agents now say it |
| 2026-09-21 | PR #322 repaired an e2e wait helper that has no caller and waits on a render tracker that cannot count (#321); its new spec then failed in CI `browser-qa`, which the builder could not run. 43 of 96 helper exports in `e2e/` turned out to have no caller | a no-dead-export check on `e2e/`; a caller audit before repairing a helper | no — #332 adds the check; #321 deletes the helper |
| 2026-09-21 | Eight agents created worktrees by hand today and every one repeated the same sequence, hitting the same failure modes each time: a branch cut from a stale base, `pnpm install` forgotten until a test failed, `husky: command not found`, Node resolving to the v14 binary | a single worktree-plus-install script (rows above already named #157 as the fix, filed after the fetch-hang and husky rows on 2026-09-18/19; today's repetition is the trigger that got it built) | yes — #157: `scripts/wt-new` |
| 2026-09-21 | A builder's `origin/main` moved mid-task on a busy day; one fetch at the start wasn't enough, and the drift was noticed only indirectly, from phantom deletions in a `git diff --numstat` | a cheap way to check freshness without a full rebase | yes — #157: `scripts/wt-new --refresh` (fetch + ahead/behind, read-only) |
| 2026-09-21 | Claude's weekly usage — a run's own stop condition — was read by looking at the rendered settings page, so it was re-read rarely, went stale for hours, and a wind-down was decided against a three-hour-old number | nothing: there was no known way to read it as data | yes — `usage-rationing.md`: the JSON endpoint behind that page, read from an authenticated tab |
| 2026-09-21 | The size budget had no idea what a PR was made of: the foundation plan (#318) removes legacy capability by capability, and a 400-line ceiling would have sliced those deletions arbitrarily, while the first research set (#137) only landed because its ~1,500 lines of evidence predated the check — the second time the budget's shape, not its numbers, was the problem (row 11 was the bot/linked-issue version) | trying the check against every *kind* of PR that exists — a removal and a research set, not only feature PRs (the same lesson as row 11) | yes — #326: deletion-only PRs are exempt (adds ≤ 5 per file, ≤ 20 total, ≤ 5% of deletions — the fix-ups a removal forces) and `docs/research/*/evidence/` is excluded; ADR-0013 Decision 3 amended |
| 2026-09-21 | Nothing recorded which model verified which PR, so a `risk:2` PR (#320) was verified a tier below its policy and merged unnoticed, and half the evidence later used to justify a tier change could not be attributed to a model at all | nothing looked: the tier was a dispatch-time choice with no trace in the PR, the repo or the API | yes — `verifier-brief.md`: every verdict carries `Verified on:`, and a verifier off its rule says so in either direction (#352) |
| 2026-09-21 | A brief invariant meant to pin the verifier's model-per-risk mapping pinned only word order: `containsPhrases` is an ordered-subsequence match, so a rewrite saying the exact opposite passed 109/109 | the invariant itself — it was a list of short phrases, all of which survive any re-tiering | yes — the mapping is pinned as one whole sentence, and a swapped tier now fails (`scripts/skills.logic.mjs`) |
| 2026-09-23 | A two-clause tier rule was made unambiguous in `verifier-brief.md` and **reintroduced in the same commit** in `.claude/agents/hacer-verifier.md`'s description — which is itself a dispatch surface, so the fix did not reach the place the coordinator actually reads | the fix touched the file the blocker named, not every file that states the rule | yes, on the second occurrence — #391; see the row below |
| 2026-09-23 | An invariant listing several phrases in one entry coupled the rule to document order: an innocent section reorder failed it with a misleading message, while a rewrite that dropped the safety-critical half passed | `containsPhrases` is an ordered subsequence, and a multi-phrase entry silently asserts their order too | yes — one phrase per entry, each pinning a whole load-bearing sentence (`scripts/skills.logic.mjs`) |
| 2026-09-21 | All 134 unit test files ran in jsdom under `src/test/setup.ts`, including the 66 that need no browser at all — so "the engine runs headless" was a claim no command checked, and a new `localStorage` call inside `src/core` or `src/store` would have been invisible | a test project that gives the headless layers no DOM to fall back on | yes — #323: a Vitest `node` project (`pnpm run test:node`), membership by directory in `vite.config.ts`, exceptions listed with their reason in `docs/testing/vitest-projects.md` |
| 2026-09-21 | The owner's "fix the foundation first" reached the backlog as a label and an epic, but the pick rule knew neither: `project:foundation` had no portfolio row, so the plan's own issues (#329, #331, #335, #336) read as `unshaped` — unpickable, with no reason a reader could act on — while every `risk:2` task the plan is about to replace still looked ready | the pick rule and `docs/portfolio.md` being one thing: a label with no row silently files work as `unshaped`, and `scripts/backlog.logic.test.mjs` only pins what the file already says | yes — #330: the `foundation` row, the amended rotation, `project:foundation` winning over label order, and the `risk:2` gate with `foundation-gate` as its stated reason |
| 2026-09-23 | HACER's three-layer rule — pure logic ← state ← rendering — was enforced by nothing: the foundation audit's 16 production cross-layer edges, 3 cycles and 33 `console.*` calls under `src/utils` all passed a green `lint`, and the foundation plan (#318) was about to move a great deal of code past them | a check on the import graph in `lint`: `tsc` guards types and `lint:docs` guards doc paths, neither looks at direction | yes — #329: `.dependency-cruiser.cjs` + `eslint.config.js` "the engine layer", both with shrink-only baselines, run by `pnpm run lint:layers` inside `pnpm run lint` |
| 2026-09-23 | The same stuck-PR cause for the sixth time (#287, #314, #343, #351, #360, #362 — essentially every PR), each costing a manual intervention. The mechanism, read off the API rather than guessed: GitHub sends one webhook action **per label**, so `gh pr edit --add-label a,b` right after `gh pr create` delivers `opened`, `labeled`, `labeled` in the same second or two and starts three `pr-hygiene` and three `browser-qa` runs; `cancel-in-progress: true` cancels one mid-run, a run cancelled *in progress* posts a `cancelled` check run under the required context name, and GitHub judges a required context by the **newest** check run with that name — so `gh pr checks` reads all green while `mergeStateStatus` stays `BLOCKED` | the 2026-09-19 row above named the cause on the first occurrence and nothing turned it into a control for four days; the ledger rule ("second occurrence → a mechanism") was not applied | yes — #295: `cancel-in-progress: false` on `.github/workflows/pr-hygiene.yml` and `.github/workflows/browser-qa.yml`. A run cancelled while still *pending* creates no job and no check run (runs 35613803628 and 35613803591: `completed/cancelled`, `jobs: 0`, absent from the commit's check-runs), so GitHub still collapses the queue to one pending run per group and the failure mode is gone rather than rarer — at the cost of one extra run per PR and no cancellation of a superseded push. Narrowing `types:` was rejected: both checks genuinely depend on labels. Guarded by `scripts/required-checks.logic.mjs`; recovery for PRs opened before it landed is in `docs/harness/README.md` § When a required check is stuck |
| 2026-09-23 | #326's deletion-only exemption met its first real deletion (#321 + #332) and did not apply: the same issue that asks for ~1,050 deleted lines also asks for the guard that keeps them deleted, and that guard is 194 lines across two files — past the ≤ 5-lines-per-file fix-up proxy — so `pr-hygiene` read FAIL at 523 reviewable lines, 517 of them deletions | trying the exemption against every *kind* of deletion before relying on it: a removal that ships its own guard is the common shape, not an edge case (the same lesson as the bot/linked-issue row and the 2026-09-21 budget row) | no — `size-override` with the reason in the PR body this time; the real fix is for the exemption to tolerate an added guard the linked issue names |
| 2026-09-23 | The same tier-rule defect appeared in `.claude/agents/hacer-verifier.md` a **second** time on the same PR (#351): round 1 reintroduced the two-clause ambiguity there in the commit that fixed `verifier-brief.md`, and round 2 was the same class again. `.claude/agents/*.md` is the one dispatch surface no invariant covers, and pinning more phrases would not have caught it — the defect is divergence between twins | `scripts/skills.logic.mjs` pins the briefs and the skills we own; it has never looked at the agent definitions that state the same rules | yes — #391: the load-bearing check is **cross-file agreement** (the tier sentence in the agent definition and in the brief are the same sentence), not a longer list of pinned phrases |
| 2026-09-23 | Recovering #295's stuck required checks by rerunning every cancelled run at once **made it worse** — each rerun contends with its sibling under the same `concurrency` group, and on #362 one cancellation became three. The coordinator's own written recovery advice was the cause, and it had never been tried under contention before being written down | nothing: the recovery was reasoned, not measured, and the ledger row that named #295's cause carried it as advice | yes — `docs/harness/README.md` § *When a required check is stuck* now says one run at a time, waiting for each; `~/.local/bin/gh-merge-on-green` (local to the owner's laptop, not in this repo) does it that way |
| 2026-09-23 | Three times in one run the coordinator wrote a criterion or a recommendation from prose rather than from the artefact, and each cost a builder or a verifier real work: *"`fast-check` is already a dependency"* on #355 (it is in neither `package.json` nor the lockfile), *"deleting `renderTracking` shrinks the layer-ratchet baseline"* on #332 (34 → 34 — `src/utils` is not one of the three layers, so it was never a violation), and a net-negative exemption threshold recommended on #389 that fails on #388's own reviewable split (`194 ≤ max(20, 69)` is false) | the rule the repo already states for issue bodies — a criterion naming a measured fact is checked against the artefact before it is written — which was never applied to the coordinator's own briefs and comments | no — three occurrences, one class, and past the ledger's own threshold; nothing is filed yet. `docs/harness/sessions/2026-09-23.md` §7 names it as the run's main process lesson |
| 2026-09-23 | A property test over part orderings would have passed **400/400** against the unfixed compiler: its generator put every writer at the same dependency depth, so Kahn's queue drained them before the reader whatever the source order. Mixing the depths gave 105/400 wrong. The builder caught it itself. Second occurrence of a green assertion that could not fail (2026-09-21: `containsPhrases` passed a rewrite saying the exact opposite, 109/109) | the red-commit step in `implementer-brief.md` — and it did, because the builder ran the property against the red commit instead of only against the fix | no — the control that caught it already exists; what is missing is that a **property** test record the failure count it saw at red, so a later vacuous generator shows up as a changed number rather than as a pass |
| 2026-09-23 | #342's criteria — hold React at 19.2.x because `@react-three/fiber`'s peer range excludes it — rested on an upstream fact that expired **~31 hours after the issue was written** (#342 created 2026-09-21T12:46:27Z; R3F 9.8.0 published 2026-09-22T19:43:58Z): `pmndrs/react-three-fiber#3916` merged 2026-09-22 and v9.8.0 widened `peerDependencies.react` to `>=19 <19.4`, so the issue's own documented lift condition was already satisfied before any builder opened it. Building it as written would have merged a pin whose stated removal condition was met, holding HACER back from a React release upstream supports. Fourth occurrence today of the class in `docs/harness/sessions/2026-09-23.md` §7 | nothing re-checks an issue's **premise at dispatch time**. The existing rule fires when a criterion is *written*; an `agent-ready` issue then sits in the backlog while upstream moves under it, and every criterion quoting a registry or a release is perishable. #342's builder caught it only by re-running the check itself | no — #394 owns the class; this occurrence adds the axis it does not yet cover: a premise **true when written and false when dispatched**. The cheap control is re-running the issue's own stated verification command before dispatch, not a new check. Residual tracked in #408 (Dependabot grouping), which fixes the ordering hazard but not this |
