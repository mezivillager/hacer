# Session record — 2026-09-24: four merges, and a defect class that keeps being caught by tools rather than readers

This is the committed memory of the 2026-09-24 session. It lets the work resume from the repo alone.
What to do next lives in `docs/portfolio.md` and GitHub Issues, never here.

## 1. The goal

Unchanged (`sessions/2026-09-18.md` §1): agents move HACER forward with very little owner oversight.
The owner's instruction for this run: *"work autonomously to do the usual coordinator loop till
claude weekly usage reachs 85%"*, ending when he called the ceiling. Publishing stayed `on-green`
(the standing grant in the workspace `CLAUDE.md`). Model policy unchanged: coordinator Opus 5,
subagents Opus for `risk:2` or any `src/core`/`src/simulation` change and Sonnet otherwise, **never
Fable**, at most **3** concurrent local agents.

The cloud lane's six rows (**#193, #338, #199, #427, #195, #333**) were left untouched by design —
`ha-next` must not steal queued work.

## 2. What landed

| # | What |
|---|---|
| #416 | L-001 — the legacy corpus is hand-built; no real document exists |
| #433 | ADR-0020 §1.5 amendments — the alias spike's correction, plus a tightening round |
| #434 | #313 — explicit per-test timeouts, sized from measurement |
| #432 | #406 — the layer-ratchet growth guard, after **three** rounds |

## 3. #432 took three rounds, and the third round is why it is trustworthy

Round 1 passed with a finding. Round 2 closed it and was **BLOCKED** for a **false PASS**:
`pr-hygiene.logic.mjs` derived "a new rule is being armed" from the **base baseline rows** rather
than from the config, and `engine-no-ui-packages` is armed with **0 of 72 rows** — so a genuine
`zustand` import under `src/core/` plus the documented `--baseline` regeneration produced
`PASS ratchet=armed`, exit 0. A **fabricated** rule name passed the same way, and the channel
**widened as the ratchet shrank** (`engine-no-ui` is down to 1 row).

Round 3 went further than the prescribed fix: a name is new **iff** it is declared in the head
config, **absent** from the base config, **and** `.dependency-cruiser.cjs` is among the PR's changed
files — because changed-files alone still let the absorbed-name attack through for one cosmetic
line while asserting a false arming.

Its verifier reproduced all four attacks failing (including a fabricated name *with* a cosmetic
config edit), and proved at file:line that the config text is **read, never executed** — which
matters because `pr-hygiene` is a `pull_request_target` workflow: `pr-hygiene.mjs:58-68` fetches raw
text, `logic.mjs:331-333` is one `matchAll` over a literal regex, and there is no
`require`/`import()`/`eval`/`Function`/`child_process`/`vm` in either script.

**Two residuals are filed as #438**, both raised as nits and neither blocking:
- The scan has **no self-check**. If `main`'s config were ever written programmatically,
  `baseConfigRules` comes back empty and **any absorbed row reads `PASS armed`** — the same false
  pass, reachable through a change nobody would think of as touching the guard. It should fail closed.
- The corrected residual is written in three places, not the four the PR claimed; it is missing from
  `.github/workflows/pr-hygiene.yml`.

## 4. ADR-0020's amendment corrected the spike, and the tightening corrected the amendment

#433's verifier re-derived all seven fixtures independently against the real `compileHDL`; the
amendment's correction of the spike holds. One of its four nits was not style, and it is the one
that matters to **#374**, the next issue to read §1.5: **the boundary write-back does not survive
composition.** Raw `P({a:1})` gives `{out:0}`; wrapped with the §1.5 write-back, `{out:1}`;
registered and instantiated as `CHIP Q { IN x; OUT y; PARTS: P(a=x, out=y); }`, **`{y:0}`** — because
`evaluateChipWithCtx` returns `compiled(inputs, ctx)` directly (`src/core/chips/evaluateChip.ts:53`).
§1.5 now says the write-back belongs inside the chip definition a spec registers (§9/#190's
`'circuit'` composite evaluator) and that a composed chip pays it again at its own boundary. Read
§1.5 alone before this change and you would put it in the caller.

Also settled honestly rather than tidily: of §1.5's three stated reasons, only the **diagnostics
cost** carries the decision — the id shift is mitigable by §1.9's append-last rule and the width gap
is neutralised by the ADR's own width-generic export rule. Both are now labelled soft. The
`Or4`/`Or8` rejection now rests on `project01.ts` registering exactly the sixteen Project-1 chips,
which makes them invented chips in the compatibility baseline.

## 5. The process lesson: the controls that worked were not reviewers

The class named in `sessions/2026-09-23.md` §7 and owned by **#394** — stating a measured fact from
prose instead of from the artefact — recurred three times in one twenty-minute stretch of the
**coordinator's own** work:

1. `gh-merge-on-green` called a mergeable PR a `REAL FAILURE` because it treated *any* red check as
   fatal. Only `ci`, `pr-hygiene` and `browser-qa` are required here; `deploy-preview` and CodeQL are
   not. The PR was blocked for an unrelated reason — 12 commits behind.
2. The fix for that shipped a **worse** bug. `gh` writes API error bodies to **stdout**, so an
   unprotected-branch 404 was read as a context name — and a required-set file holding only that junk
   is non-empty, so the script would have believed it knew the required set and treated **every** real
   failure as non-blocking. A fix against refusing good merges nearly became one that accepted bad ones.
3. An issue was one step from being filed claiming `deploy-preview` is broken on docs-only PRs. On
   check: #432's head had **zero** failing checks, and the failures were on #416's pre-rebase head
   against a stale base. Not filed.

**What caught them.** (2) was visible only because the tool **printed the premise it had concluded**;
(3) because the same check was run against a **second** PR. Neither needed a reviewer. Each needed
one more measurement before the claim.

The generalisable control is (2): make a tool state the premise it is acting on, out loud, where the
next reader sees it. It is cheaper than review and it fires every time. The same pattern caught the
verifiers' finds this session — #432's "stated in all four places" claim fell to opening the fourth
file, and #434's builder claim about the line budget held up only because the brief was read.

## 6. Also filed

- **#435** — nothing detects a test getting slower. #434's 10s budget would hide a mount degrading
  from 2.4s to 9s. Explicitly **not** a #434 regression: the old 5s default had the same blind spot,
  narrower. A naive duration assertion will not work — measured variance on the same test across two
  consecutive solo runs was **3–9x** (3177ms then 771ms) — so the note points at trend-over-CI.
- **#436** — `docs/testing/vitest-projects.md` omits the `.mts`/`.js` globs and the deliberate
  node-project asymmetry (a `.mts` file under a `NODE_TEST_DIR` lands in jsdom). Zero files use those
  extensions today, so it is a trap to document, not a behaviour change.
- **#438** — #432's two residuals, above.

## 7. State at close

**Open and owed work:**

- **#437** (`fix/402-dropped-entity-warnings`) — #402's dropped-wire/dropped-junction warnings.
  Green, DoD all exit 0, **not fresh-context verified**. The builder was stopped at the ceiling with
  the implementation green but uncommitted; the coordinator ran the definition of done, added the
  ledger row and opened the PR. `claim/402` is left in place on purpose. A PR comment names the three
  things to point a verifier at. **Verification is the only thing it needs.**
- **#180 is deliberately deferred.** It shrinks the ratchet baseline, and it was held back while #432
  — whose entire subject is baseline arithmetic — was in flight, to avoid the #407 green-alone /
  red-merged pattern this repo has already paid for once. With #432 merged it is a clean pick.
- Dependabot **#410–#414** still need triage (**#415** belongs to the cloud lane as #427).

**How to resume:** `node scripts/backlog.mjs ready`, then `docs/portfolio.md`. The carried queue was
#180, #403, #407, #391, #390, #400/#371, #405, #389, #148, #152, #253/#290, #315, #217.

---

## 8. Second run, same day: 85% → 87%, stopped by the owner for local resources

The owner restarted the loop with a new ceiling of **93%** weekly. The run ends at 93% or at the weekly
reset, whichever comes first. He stopped it at **87%** because he needed the laptop's resources back.

### Landed

| # | What |
|---|---|
| #444 | #199 parser fuzz, built by Grok Bot's cloud agent and verified here. The verifier found the test sound but **blocked on history**: the red commit had broken production `parseHDL`, and a `fix(hdl):` commit would have cut a release for a bug that never shipped. `main-rules` allows rebase only, so the branch was squashed into one `test(hdl):` commit with an identical tree. |
| #437 | #402 dropped wire/junction warnings. |
| #451 | #180 `signalDisplay` layering, plus #450. This was the **first ratchet shrink since #432**: the guard read `shrank` (72 → 71), and `engine-no-ui` is now at **0 rows**. It also exposed #450, a test that read the live baseline and asserted 72, so it broke on the first shrink. |
| #413, #414, #411 | Dependabot: Radix and jsdom 30. |

**#410 was held.** It pairs React 19.3.0 with R3F 9.7.0, whose peer range is `>=19 <19.3`; this was
checked against the registry. Every check was green anyway, because `pnpm install` only warns on a peer
violation. `@dependabot recreate` was posted, since 9.8.0 admits `<19.4`. This is the second time a peer
range has decided a React merge, so it is filed as **#455**.

### Open when stopped, with what each is owed

- **#459** (#403, `removeJunction` keeps every wire with two real endpoints; option 2 was ruled in the
  brief). It is built, and the red test runs through the real gesture: only 1 of 3 wires survived
  before the fix, 3 of 3 after. **Its verification was interrupted, so it has no verdict.** It needs a
  complete fresh-context verification before merge.
- **#461** (#438, the ratchet guard fails closed as `ratchet=unreadable`). It is built. **Its
  verification was interrupted**, so it needs a complete one, including a live-or-latent ruling on
  #456. #460 edits the same test file and waits for it.
- **#182**: only the red commit exists (`2ddfc4c`), pushed to `fix/182-chip-completion-to-session`.
  `claim/182` is held, and the issue carries an `Intent: paused` note.

### Filed

#452 (an empty junction is reported with a false reason) · #453 (HDL pin width has no upper bound) ·
#454 (the implementer brief has no path for test-only issues) · #455 (CI ignores peer-range violations)
· #460 (two more tests decay as the ratchet shrinks, one of them silently) · #462 (removing a junction
from an imported document still loses its connection).

### The owner's project ideas: parked, not work

**#457** (decision-flow tracking) and **#458** (a control-center site) carry the new `idea` label and
**no** `agent-ready`. They are never picked or shaped, and they move only when the owner asks.

### What recurred

A test that reads live state and asserts a constant broke the moment the ratchet did its job. It
happened three times in one day (#450, then both tests in #460). Separately, two builders found that
another agent had overwritten their logs in the shared scratchpad. Both re-ran before quoting a
number. From now on, briefs name a per-task log folder.
