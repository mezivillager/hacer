# Rulings — 2026-09-24 loop-93

Imported by #466 on 2026-09-25 from the coordinator's run directory `2026-09-24-hacer-loop-93`; wording unchanged except machine paths. `Builds on: unknown` marks a ruling nobody has annotated; a restated ruling carries a new id and `Amends:` the original.

## R600 — the stop condition is 93%
Builds on: R516
or the weekly reset**, whichever comes first
The owner said "until weekly usage reaches 93%". At start: `seven_day` **85%**, resetting
**2026-09-25T03:00Z** — ~14.7 h away. If the week resets first, the meter drops to near zero and a
literal "until 93%" would mean spending most of *next* week's allowance, which is plainly not the
instruction: it is about this week's remaining room. So the run ends at 93% or at the reset.
**Cost if wrong:** the owner wanted it to carry on into the new week and has to say so. One line.
**Cost of the opposite:** a week's budget spent on an instruction about eight points of one.


## R601 — verification owed comes before new building
Builds on: none
Two PRs are built and unverified: **#437** (#402 — mine, finished by the coordinator at the last
ceiling, never independently read) and **#444** (#199 — built by Grok Bot's cloud agent, whose inbox
row hands `src/core/hdl` verification to local Claude and says Grok Bot will not merge). Both go
first. An unverified PR ages against a moving `main`; a verification is also the cheapest way to turn
already-spent work into a merge.
**Cost if wrong:** a new build starts one slot later. Negligible.


## R602 — the harness loop is this workspace's prompt-it pipeline
Builds on: none
Each item runs the way the last three runs did: an issue-scoped builder briefed against
`docs/harness/implementer-brief.md` (TDD, red commit first, five DoD commands), then a fresh-context
verifier against `verifier-brief.md`, then merge on green. That *is* the hacer pipeline;
`ha-prompt-it`'s approval gate is the one the owner delegated ("decide, record, surface only true
blockers"). Tier **Light** for every item bigger than a one-liner; none opens a new cross-system
contract except #403, which is still one PR.
**Cost if wrong:** an item that wanted a Full plan gets a Light one, and the verifier says so.


## R603 — Grok Bot's lane: two rows still theirs
Builds on: R500
The inbox (as rewritten by #440) shows #427, #195 and #333 **done**; **#193** still building (Project 1
merged as #443, Projects 2–5 open) and **#338** queued. `backlog.mjs ready` lists #338 and #199 anyway —
it does not read the inbox. Neither is picked. #199's *verification* is picked because the row
explicitly hands it to local Claude.
**Cost if wrong:** none — skipping is the safe side of a double builder.


## R604 — #444's BLOCK resolved by squashing, not by a builder round
Builds on: R601
The #444 verifier (Opus) found the fuzz test sound and blocked on **history**: the branch carried
`d73ad60` (production `parseHDL` shifted +100_000 to show the red) and `5a1dbbe` "fix(hdl): restore…".
`main-rules` allows **rebase only** — squash-merge is refused by the ruleset — so a merge would have
landed a broken-parser commit on `main` and a `fix:` that semantic-release rates a patch, cutting a
"Bug Fixes" release for a bug that never shipped.
Resolved by rewriting the branch into one `test(hdl):` commit. Tree before = tree after
(`6fb2ddf…`), checked by id; net diff = the one new test file; the red demonstration preserved in
the commit message. The branch is Grok Bot's cloud build, but its inbox row hands verify-and-merge to
local Claude and says Grok Bot will not merge, so the coordinator of record did it.
**Cost if wrong:** the red commit is gone from `main`'s history. It is preserved on the PR, in the
commit message, and in the verifier's independent reproduction (17/21 injected breaks caught).
**My own slip on the way, recorded:** the first attempt created the worktree *inside* the repo
(`git -C hacer worktree add <relative path>` resolves against `hacer/`), and my tree-equality guard
printed "identical" with **both ids empty** — a vacuous pass in the very check meant to prove the
squash was safe, one message after the verifier had ruled the fuzz test non-vacuous. `set -e` and it
aborted before anything was pushed; the rerun guards against empty ids explicitly.
Root cause filed as **#454** (the brief has no path for test-only issues).


## R605 — #437 merges on PASS; its nits are a follow-up, not a round
Builds on: R601
PASS with four criteria met and the all-vs-some junction question answered at file:line. One nit is a
user-visible false *reason*: a junction saved with `wireIds: []` now reports "left dangling by gates…
that could not be loaded" when nothing was dropped (previously it was dropped silently). Rare, and the
fix is a design choice (prune silently, or its own code), so filed as **#452** rather than patched
into a verified PR. Also on me: the PR body named `test:run` as the issue's verification command;
the issue's command is a scoped vitest run. #394 again — written from memory while finishing the
builder's work.
**Cost if wrong:** a rare, misleading toast lives on `main` until #452 lands.


## R606 — #450 folded into #451 rather than its own PR
Builds on: none
#451 (#180) is the first baseline shrink and turned `test:run` red because #432's test at
`pr-hygiene.logic.test.mjs:651` pins the **live** count (72/73). The #180 builder correctly refused to
edit #432's test inside #180 and filed #450. Ruling: fold the one-line fix into #451 — it is the change
that exposes it, a separate PR costs a builder+verifier cycle for one line, and #451 would otherwise be
unmergeable until it landed. The #438 builder, editing the same file, was told not to add any
live-count pin and to keep its edits local.
**Cost if wrong:** #451's verifier reviews two concerns in one PR. Small, and they are causally linked.
**Class:** this is #407's green-alone/red-merged shape again, from a different direction — a test that
reads live state and asserts a constant. It surfaced only because the ratchet moved for the first time.


## R607 — #403 takes option 2, ruled in the brief
Builds on: ADR-0020 §7.5b
The issue lays out three contracts and recommends option 2 (delete only wires whose `from`/`to` is
the junction; keep every wire with two real endpoints). Taken: it matches the defect (valid wires are
deleted), needs no schema field, and option 1 would grow the schema of a construct ADR-0020 §7.5b is
retiring. The builder is told to stop and report — not switch options — if keeping a standalone wire
proves wrong (duplicates the evaluator or renderer cannot handle).
**Cost if wrong:** a kept wire overlaps its re-drawn twin; visible, recoverable by deleting it, and far
cheaper than silently deleting a user's wiring.


## R608 — Dependabot: merge the three leaf bumps on green, hold the React group
Builds on: none
Assumes: P-001
Portfolio row 11 sets the standard ("0 stale dependabot PRs") and CI is their verifier. #413 and #414
(Radix patch/minor) and #411 (jsdom 29 → 30, test-environment major) merge sequentially on green —
sequential because `strict` required checks put every other green PR `BEHIND` on each merge (ledger,
2026-09-18).
**#410 held.** It pairs `react` 19.3.0 with `@react-three/fiber` 9.7.0, whose peer range is
`>=19 <19.3` — **checked on the registry, not recalled** (9.5.0 and 9.7.0 `<19.3`; 9.8.0 `<19.4`).
I had the fact from #342's ledger row and nearly acted on it from memory; given four #394 instances
yesterday, I looked it up first, and it held. `@dependabot recreate` posted — 9.8.0 is latest and
should be picked. Every check on #410 was green, because `pnpm install` only warns on a peer violation.
That is the **second occurrence** of a peer-range fact deciding a React merge (#342 first), so it is
filed as **#455** to become a red check.
**Cost if wrong (holding #410):** React 19.3 lands a few hours later. **Cost of merging it:** an
unsupported reconciler/React pairing on `main`, invisible to every check we run, in a 3D app we never
render on this machine.


## R609 — the owner's two project ideas parked as `idea` issues, outside the run's queue
Builds on: none
Mid-run, the owner pushed two ideas "so I don't forget about them" and said they are not to be picked
up until he asks: decision-flow tracking (**#457**) and a control center site (**#458**). Filed with a
new `idea` label, **no** `agent-ready`, his words quoted, pointers only, and a banner forbidding
shaping or picking. Verified, not assumed: `backlog.mjs ready` lists them only in its trailing
not-pickable section as `unshaped`, which `ha-next` treats as "stop". **Not** added to this run's queue.
Not mechanised further: making `backlog.logic.mjs` report an `idea` reason instead of `unshaped` would
stop a shaping sweep from seeing them as unfinished work, but it is a code change during a run the
owner scoped, and the banner already covers it.
**Cost if wrong:** a future shaping sweep reads past the banner and shapes one. Visible in the issue
history, and reversible by removing a label.


## R610 — #451 merged; its verifier's two further decay hazards filed as #460, and #460 is held behind #438
Builds on: R606
#451's Opus verifier passed it after two independent pr-hygiene readings (`shrank`, 72 → 71) and four
tampered variants failing as they should. It found the #450 fix did not cover two more tests that read
the live baseline: `pr-hygiene.logic.test.mjs:851` goes **vacuous** at 0 `engine-no-state` rows, and
`:613` goes red at 0 `core-through-index` rows. Filed as **#460**. Third instance of the class in one
day (#450, and these two): a test on a clock set by the ratchet's own success.
**#460 is not dispatched yet** — the #438 builder is editing the same test file, and two concurrent
PRs in one file is the #407 shape. It goes out once #438 merges.
**#182 dispatched instead**, after checking rather than assuming: the 9 `engine-no-state` rows are
serialization/topologicalEval/truthTable → `store/types`/`circuitStore`, none of them
`chipCompletion`; #182 removes a browser-global (`localStorage`), not an `engine-no-state` row. So it
cannot trip #460's silent hazard; `core-through-index` is at 43.
**Cost if wrong:** #182 empties a rule unexpectedly and a test goes vacuous; the builder is told to
report, not patch around, and #460 then goes first.

Usage 13:42Z: `seven_day` **87%**, `five_hour` 7% — +2 points since 12:16Z across ~7 Opus agents.


## R611 — agents get a unique scratch folder, stated in every brief from here
Builds on: none
Two builders reported the same thing independently today: another agent overwrote their `test.log` in
the shared session scratchpad (#180's builder by a #444 verifier; #438's builder by another builder).
Both noticed and re-ran before quoting a number, which is why no wrong figure reached a PR body. But
the next one might not notice, and a number read from someone else's log is the #394 class arriving
through the filesystem instead of through memory. From R611 on, every brief names a per-task log
folder (`$TMPDIR/<role>-<issue>/`).
**Cost if wrong:** none — a longer path in each brief.
Also noted from #461's builder: 3 jsdom UI tests timed out once at load average ~150 and passed on
rerun. That is roughly 3x the load #313's timeouts were sized against; #435 (nothing detects
slowness) already covers the shape. Not acted on.
