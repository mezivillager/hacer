# Decision Lineage research — 2026-09-24

The owner asked for *"tracking decision flow … a wrong decision was made at step 1 … the decisions
that built upon that step 1 decision have reached step 100 … we should be able to associate it with
the decision that introduced it, and then find out all other decisions that wrongly built upon it"*,
parked as #457, and then asked for it to be researched so tasks could be queued on equal footing with
the other priority projects — and for a better name.

| Read | For |
|---|---|
| `REPORT.md` | the name (§2), where decisions live today and how they link, measured (§3), four real chains reconstructed as test data (§3a), why the control the ledger proposed for the #342 class would not have fired (§3b), options (§5), the model and its capture format (§6), the operations including the step-100 query (§7), the epic and tasks (§8), decisions taken on the owner's behalf (§9), the pick-rule slot for both new projects (§10), open questions with defaults (§11) |
| `../2026-09-24-mission-control/REPORT.md` | the sibling project, which renders this graph |

Status: research only. Epic, label, rulings home and tasks are filed after the owner reviews. The
measurements were taken at `origin/main` `97f109b` and are not maintained; the rulings counted live in
the coordinator's run directories outside the repo.
