# Failure ledger

One line per thing that went wrong in the loop. The **second** time the same thing appears, it
becomes a mechanism — a lint, a test, a hook, a check — and the row links to it. Only what cannot be
mechanised goes into `AGENTS.md`, under its line budget.

Format: `date · what went wrong · which layer should have caught it · mechanised? (link or "no")`

| Date | What went wrong | Should have been caught by | Mechanised? |
|---|---|---|---|
| 2026-09-18 | Four statements in the workspace `CLAUDE.md` were stale for months (HDL pipeline "not wired", `gateLogic.ts`, Stryker, E2E in the DoD); every session started from a wrong picture | a does-this-path-exist check on cited files (#153) | no — #153 |
| 2026-09-18 | The `main-rules` ruleset could only be merged through by admin bypass (1 approval a solo owner cannot give; `required_deployments` and `code_scanning` rules no PR could satisfy); AGENTS.md claimed CI was required — it was not | nothing; a settings audit | yes — ADR-0013; ruleset rewritten, bypass removed after #234 |
| 2026-09-18 | First research draft kept the owner as the merge bottleneck and proposed label gates the owner's own identity defeats | an adversarial review before acting | yes — every research deliverable gets a fresh-context adversarial review + fact-check |
| 2026-09-18 | Research draft rested a goal-post exit test on a `lint:docs` capability that does not exist (it only rejects absolute paths) | fact-check | yes — reviews; the capability itself is #153 |
| 2026-09-18 | Tracer PR #238: the pre-commit `tsc -b` makes a compile-broken "red" commit impossible; the builder committed the test file from a green tree instead | `ha-prompt-it` should say how to write a red that compiles | no — fix in v2 (#154): red = test + a compiling stub that fails, never `--no-verify` |
| 2026-09-18 | `pnpm install` in every worktree prints `husky: command not found` (husky is not a devDependency; hooks still run via `core.hooksPath`) | the worktree script (#157) | no — #157 |
| 2026-09-18 | Tracer PR #238: 11 builder tests + a green DoD missed that the engine read leftover pin state (a table could differ before and after save/reload); the fresh-context verifier found it with a repro and a 2-line fix | the verifier — and it did | yes — `verifier-brief.md` step 6 ("try to break it") is why it was found; keep it |
| 2026-09-18 | Issue #149's verification command named an exact new test file (`scripts/backlog.test.mjs`); the builder followed the repo's `x.logic.test.mjs` convention, so the command as written exited 1 | the issue form: verification commands should target a directory or pattern, not a file that does not exist yet | no — add to the issue-form guidance (#154) |
| 2026-09-18 | Ticket P05-31 (6 h, engine + store + UI) would have been one ~600-line PR under the old rule; split into #164/#235/#236 it shipped as a 163-line PR in 34 minutes | ADR-0013 one-sub-issue-per-PR | yes — ADR-0013; `pr-hygiene` (#150) will enforce the budget |
| 2026-09-18 | `strict` up-to-date policy: every PR needs a rebase + fresh CI (~2 min) once `main` moves; with three PRs landing in an hour that was three extra CI runs | expected; a cost, not a defect | n/a — revisit if it becomes the bottleneck |
| 2026-09-18 | `pr-hygiene` was made a required check after being tried only on human PRs; every dependabot PR then failed its linked-issue rule | trying a new required check on every *kind* of PR that exists (human, agent, bot) before requiring it | yes — bot/`dependencies` exemption (#250); the rule itself: ADR-0013 amendment candidate |
| 2026-09-18 | Removing the admin bypass broke every release: `@semantic-release/git` pushes a commit to `main`, now rejected (`GH013`); five release runs failed before anyone looked | an audit of every workflow that pushes to `main` before changing the ruleset; `agent-orient` showing the release workflow's status | partly — ADR-0015 (releases no longer commit to `main`); release status in `agent-orient` is #156 |
| 2026-09-18 | The agents' `gh` token lacked the `workflow` scope, so they could not update or auto-merge any PR touching `.github/workflows/**` (dependabot #118/#131, and a cosmetic `ci.yml` hunk in #245) | a token-scope check at session start | yes — owner ran `gh auth refresh -s workflow`; agent PRs still avoid incidental workflow edits |
| 2026-09-18 | `strict` required-status-checks made every merge put every other green PR `BEHIND`; ten PRs in two hours meant a rebase + CI cycle each, and dependabot PRs re-triggered each other | measuring merge throughput after the ruleset change | yes — `strict` off; `ci.yml` still runs on every push to `main` |
| 2026-09-18 | PR #244 added an AGENTS.md line citing `pr-hygiene.logic.mjs` without its `scripts/` prefix; the path-exists check (#245) caught it as soon as its branch was rebased onto that `main` | the path-exists check — before it had even merged | yes — ADR-0014 |
| 2026-09-18 | The eslint-plugin-react-hooks 7.1.1 bump (dependabot #114) failed lint with four real "Cannot access refs during render" findings | the React Compiler lint rules, on the bump itself | no — #256 fixes the code; the bump waits on it |
| 2026-09-18 | The browser-qa builder kept polling its own CI after its PR merged (~300k tokens) | the implementer brief | yes — "agents stop after opening their PR" (Budgets, #292) |
| 2026-09-19 | A builder's `git fetch` hung (a ref-lock race with another worktree fetching at the same moment); the agent sat idle until it was stopped and resumed | a fetch timeout in the worktree script | no — #157 |
| 2026-09-19 | The default subagent model was switched to Sonnet to cut cost without asking; the owner corrected it (Opus for significant work, Sonnet only for routine work) | asking before changing a cost-versus-quality knob | yes — Budgets table + `CLAUDE_CODE_SUBAGENT_MODEL=opus` (#292) |
| 2026-09-19 | #287 stayed `BLOCKED` with every required check green: a PR created with labels fires `opened` and `labeled` together, `cancel-in-progress` cancels one run per workflow, and the cancelled `pr-hygiene`/`browser-qa` contexts counted as failures | trying a new required check on a PR created with labels | no — #295 (workaround: `gh pr update-branch --rebase`) |
| 2026-09-19 | The first cloud-spike routine did all its work, then could not push (403: the Claude GitHub App had no write access), so its commit died with the container | a write-access check before a long cloud run | yes — a one-minute write-access routine now comes first; `docs/harness/cloud-session-spike.md` |
| 2026-09-19 | Connecting Cursor to GitHub switched on Bugbot (billed per use) for every PR without the owner finding the setting; it reviewed #294 | checking what an integration enables right after connecting it | no — the owner turns it off; the repo side goes with #258 |
